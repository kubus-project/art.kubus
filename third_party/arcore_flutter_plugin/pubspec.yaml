name: arcore_flutter_plugin
description: Flutter plugin for ARCore SDK, Android platform to build new augmented reality experiences
version: 0.1.0
homepage: https://github.com/giandifra/arcore_flutter_plugin

environment:
  # Upper bound widened from <3.0.0: pub already applies the Dart 3
  # "<3.0.0 means <4.0.0" leniency during resolution, but custom_lint's
  # workspace environment merge does not, and fails on the raw constraint.
  sdk: '>=2.14.0 <4.0.0'
  flutter: '>=2.0.0'

dependencies:
  flutter:
    sdk: flutter
  vector_math: ^2.1.0
  meta: ^1.3.0
  equatable: ^2.0.3

# For information on the generic Dart part of this file, see the
# following page: https://www.dartlang.org/tools/pub/pubspec

# The following section is specific to Flutter.
flutter:
  # This section identifies this Flutter project as a plugin project.
  # The androidPackage and pluginClass identifiers should not ordinarily
  # be modified. They are used by the tooling to maintain consistency when
  # adding or updating assets for this project.
  plugin:
    platforms:
      android:
        package: com.difrancescogianmarco.arcore_flutter_plugin
        pluginClass: ArcoreFlutterPlugin

