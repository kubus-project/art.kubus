part of 'backend_api_service.dart';

// Recovery/backup transport owns JWT-authenticated wallet backup reads/writes,
// passkey backup registration/authentication, and backup security events.
// Existing facade methods delegate to backend_api_service_auth_account_helpers.
